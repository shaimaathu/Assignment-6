name: app_github_connection
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.4.0-99.1.beta

dependencies:
  flutter:
    sdk: flutter
  get_storage: ^2.1.1
  flutter_bloc: ^8.1.4
  flutter_dotenv: ^5.1.0
  get_it: ^7.6.7
  supabase_flutter: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - .env